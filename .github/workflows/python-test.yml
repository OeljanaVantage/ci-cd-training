name: Python package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: 3.10.4

    steps:
      - name: Test with pytest
        run: |
          pytest